using System;
using System.Collections.Generic;
using System.Text;

namespace NoobsGameOfLife.Core.Extensions
{
    public static class IAsyncEnumerableExtension
    {
        //public static async IAsyncEnumerable<T> ForEach<T>(this IAsyncEnumerable<T> enumerable, Action<T> action)
        //{
        //   await foreach (var item in enumerable)
        //    {
        //        action(item);
        //    }

        //    return enumerable;
        //}

        //public static async IAsyncEnumerable<T> OnEach<T>(this IAsyncEnumerable<T> enumerable, Action<T> action)
        //{
        //    await foreach (var item in enumerable)
        //    {
        //        action(item);
        //        yield return item;
        //    }
        //}
    }
}
